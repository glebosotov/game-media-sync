[project]
name = "game-media-sync"
version = "0.1.0"
description = "Project for syncing Steam, PS5, and Nintendo Switch media to Immich"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["requests>=2.32.0", "pillow>=11.0.0", "python-dotenv>=1.0.0"]
